👗 Virtual Wardrobe (AI 虛擬衣櫥)

Virtual Wardrobe 是一個基於網頁的 AI 虛擬試穿應用程式。使用者可以上傳自己的衣服與人像照片，並利用 Google Gemini 強大的圖像生成能力，一鍵生成逼真的試穿效果圖。支援雲端同步、跨裝置使用與穿搭日誌保存。

(建議替換為您實際的 App 截圖)

✨ 主要功能

☁️ 雲端同步：整合 Firebase Firestore，所有衣物、照片與穿搭紀錄皆儲存在雲端，換手機或電腦也能無縫接軌。

🔐 會員系統：支援 Email/密碼註冊與 Google 帳號快速登入。

🧥 衣櫥管理：輕鬆上傳並分類上身、下身與配件，打造專屬數位衣櫥。

🤖 AI 試穿生成：

標準棚拍：自動去背並生成乾淨的棚拍風格。

最小變化：保留原圖背景與姿勢，僅替換衣物（適合保留臉部特徵）。

自訂指令：可輸入文字 Prompt 指揮 AI 生成特定場景（如：在海邊、喝咖啡）。

❤️ 穿搭日誌 (Lookbook)：將滿意的試穿結果保存，並記錄當時使用的單品，隨時回顧。

📱 響應式設計：完美支援手機與電腦版面。

🛠️ 技術棧

前端：HTML5, React 18, Tailwind CSS (透過 CDN 引入，單一檔案即可執行)。

後端/資料庫：Google Firebase (Authentication, Firestore)。

AI 模型：Google Gemini API (gemini-2.5-flash-image-preview)。

🚀 快速開始

本專案設計為 單一 HTML 檔案，部署極為簡單。

1. 準備環境與金鑰

在使用前，您需要申請以下兩個服務的設定：

A. Google Firebase (資料庫與登入)

前往 Firebase Console 建立新專案。

Authentication (驗證)：

啟用「電子郵件/密碼」。

啟用「Google」登入 (記得在設定中將您的網域加入授權清單)。

Firestore Database (資料庫)：

建立資料庫 (建議選 asia-east1 台灣節點)。

設定安全性規則 (請參考下方「安全性規則」章節)。

專案設定：

新增 Web 應用程式，複製 firebaseConfig 物件內容。

B. Google AI Studio (生成模型)

前往 Google AI Studio。

申請一組 API Key。

注意：需確保該 Google Cloud 專案已連結帳單帳戶以使用 Image 生成模型（依 Google 政策而定）。

2. 設定程式碼

打開 index.html，找到以下區塊並填入您的 Firebase 設定：

// 請到 Firebase Console -> 專案設定 -> 複製 sdk config
const YOUR_FIREBASE_CONFIG = {
    apiKey: "AIzaSy...",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project",
    storageBucket: "your-project.firebasestorage.app",
    messagingSenderId: "...",
    appId: "..."
};


注意：API_KEY 不需要寫在程式碼中，請透過 App 介面設定。

3. 部署

GitHub Pages (推薦)：將 index.html 上傳至 GitHub Repository，開啟 Pages 功能即可使用。

本地執行：直接用瀏覽器打開 index.html (部分功能可能受限於 CORS，建議使用 Live Server 或部署到網路上)。

🛡️ Firestore 安全性規則 (必要)

為了確保資料安全並讓 App 正常運作，請務必將 Firebase Firestore 的規則 (Rules) 修改如下：

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 1. 使用者資料隔離：只允許使用者讀寫自己的資料
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 2. 全域設定：允許已登入使用者讀取 API Key (建議設定好後關閉寫入權限)
    match /app_config/secrets {
      allow read: if request.auth != null;
      allow write: if request.auth != null; // 設定完成後建議改為 false
    }
  }
}
